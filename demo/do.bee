+-----------------------------------------
       repetitive statement demo
-----------------------------------------+
#driver "do"
make a := [Z](10);     -- array of 10 Words
make n := a.count()-1; -- index of last element
-- use range for iteration
make i @ [0..n];
while (i ≤ n):
   print ('r[{0}] := {1}' <+ (i, a[i]));
   alter i += 1;
repeat;
over.