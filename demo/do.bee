+-----------------------------------------
       repetitive statement demo
-----------------------------------------+
#driver "do"
array a := [Z](10);      -- array of 10 Words
value n := a.count()-1; -- index of last element

-- use range for iteration
value i ∈ [0..n];
cycle:
   print ('r[{0}] := {1}' <+ (i, a[i]));
   stop if (i = n);
   alter i += 1;
cycle;


over;