+-------------------------------
   "if" conditional execution
-------------------------------+
load $pro_lib.console:(*);

method main():
  write "define a number:";
  make x ∈ N; read x; 
  
  ** one of this will print
  print "x = 0" if x = 0;
  print "x ≠ 0" if x ≠ 0;
return;
#end of module
