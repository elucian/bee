+--------------------------
   Input a value demo
--------------------------+
rule main:
  a ∈ N;
  print "to stop enter 0";
  while (a ≠ 0) do
    write "enter a number ≠ 0:"
    read  a, "#(n)";
    print ("you have enter #(n)" ? a);
  repeat;
return;

#end of module
