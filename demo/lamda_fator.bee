+-----------------------------------
    recursive lambda expression 
-----------------------------------+
load $pro_lib.console:(*);


factor: λ(n ∈ N) => (1 if n = 0, factor(n-1)) ∈ N;


rule main:
   claim factor(3) = 6;
   print "pass"
return;