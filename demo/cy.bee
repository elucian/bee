+--------------------------------
| Demo for repetitive statement |
+-------------------------------+
driver cy:

** declare array of 10 integers
make a ∈ [Z](10);

** use repetitive statement while
make  i ∈ Z;
while i ≤ 9 do
  alter a[i] := i; // assign index
  alter i += 1;    // increment
repeat;

over. // end of driver
