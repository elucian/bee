+------------------------------------
|  asynchronous call using "begin"  |
+-----------------------------------+
driver ac:

# suspend for 2.5 sec
rule test(n ∈ N):
  wait n; 
  print ".";
return; ** test

# start 8 threads
make i := 0; 
while (i ≤ 8) do
  begin test(i);
  alter i += 1;
repeat;
rest; ** synchronize

over.
