+----------------------------------
  declare and use a λ expression
----------------------------------+
#driver "fn"

** simple λ expression
def add(a, b ∈ Z) ∈ Z => (a + b);

-- call λ expression add
put add(1, 2);     -- print 3
put add(-10, 10);  -- print 0
write;

** recursive expression
def fac λ (n ∈ N) ∈ N => (1 if n = 0, fac(n-1));
put fac(3); -- expect 6
write; 
 
over;