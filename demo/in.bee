+--------------------------
   Bee input value demo
--------------------------+
load $pro_lib.console.(*);

rule main():
  make a ∈ N;
  print "to stop enter 0";
  when (a ≠ 0) do
    read  (a, "enter a number: #(n)");
    print ("you have enter:> #(n)" ? a);
  loop;
return;
#end of module
