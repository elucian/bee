+-----------------------
| repetitive statement |
+----------------------+
rule main:
  cycle:
    x := 10 ∈ Z; 
  do
    write x + ',';  -- write in buffer
    x -= 1;   -- decrement
  repeat if x > 0;
  print; -- avered: 10,8,7,6,5,4,3,2,1,0
return;

